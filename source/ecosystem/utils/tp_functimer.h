#pragma once

/*
#include "../utils/tp_common.h"

#include <iostream>
#include <ctime>
#include <ratio>
#include <chrono>

using namespace std::chrono;

class FunctionTimer {
public:
	FunctionTimer(string name);
	virtual ~FunctionTimer();

	static void init();
	static void dump();
protected:
	string m_name;
	high_resolution_clock::time_point m_start_time;

	static VDict ms_records;
	static mutex ms_mu_ftimer;
};
*/