#pragma once

/*
#include "../support/vcommon.h"

#include <iostream>
#include <ctime>
#include <ratio>
#include <chrono>

using namespace std::chrono;

class VFunctionTimer {
public:
	VFunctionTimer(string name);
	virtual ~VFunctionTimer();

	static void init();
	static void dump();
protected:
	string m_name;
	high_resolution_clock::time_point m_start_time;

	static VDict ms_records;
	static mutex ms_mu_ftimer;
};
*/